#version 460

layout(set = 0, binding = 2, rgba8) uniform writeonly image2D ge_Canvases[];

layout(push_constant) uniform push_constants {
  layout(row_major) mat4 ge_View;
  layout(row_major) mat4 ge_Projection;
};

layout (local_size_x = 8, local_size_y = 8, local_size_z = 1) in;

void main() {
  imageStore(ge_Canvases[0], ivec2(gl_GlobalInvocationID.xy), vec4(0.0, 1.0, 0.0, 1.0));
}